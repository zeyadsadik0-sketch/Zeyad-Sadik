# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Status

This repository is currently empty. It contains only a `README.md` with the project name (`Zeyad-Sadik`) and no source code, build configuration, dependencies, or tooling.

Because there is no codebase yet, there are no build, lint, test, or run commands to document, and no architecture to describe. When code is added, this file should be updated to capture:

- Commands used to build, lint, test, and run the project (including how to run a single test).
- High-level architecture spanning multiple files — module boundaries, data flow, and any non-obvious conventions.
- Any rules from `.cursor/rules/`, `.cursorrules`, or `.github/copilot-instructions.md` if they are introduced.

## Working in This Repo

- Default development branch for Claude-authored changes: `claude/add-claude-documentation-0YjOj` (see task instructions for current branch).
- The `main` branch exists but has no code beyond the README.
